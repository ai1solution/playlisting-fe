import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2b2b2b', // black
      // main: '#FFFFFF', // white
    },
    secondary: {
      main: '#3bb371',
    },
  }
});

export default theme;